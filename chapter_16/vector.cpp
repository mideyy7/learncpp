
/*


NOTES:

Length is the number of elements currently in use.
Capacity is the number of elements that have storage allocated.

1. .size() and .capacity() of vectors
2. .resize(). ... shrinking and enlarging changes length.....only enlarging changes capacity i.e capacity does not change when shrinking
3. if you want to force capacity to change with shrink use .shrink_to_fit()










*/